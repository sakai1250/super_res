import SwiftUI

// 参考用の空画面（例示ファイル名）
// 実ナビは HomeView / PhotoListView / PhotoDetailView / SuperResolutionView で構成
struct EditorView: View {
    var body: some View {
        Text("Editor Placeholder")
            .navigationTitle("編集")
    }
}

